{
  "name": "ts-hot-reload",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "docker-compose up -d",
    "debug:server": "npm run debug -C server",
    "debug:client": "npm run debug -C client",
    "start:server": "npm run start -C server",
    "postinstall": "npm install -C client && npm install -C server"
  },
  "dependencies": {
    "tslib": "^2.1.0"
  },
  "devDependencies": {}
}
